import React from "react";

const Footer = () => {
    return (
        <footer>
            <p>Made using React, Gatsby, TypeScript, CSS, and a JSON file.</p>
        </footer>
    );
}

export default Footer;